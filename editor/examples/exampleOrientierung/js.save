button.onClick(function(){
  orientation.start(function(x, y, z){
    output.clear();
    output.addLine("X-Drehung: "+x);
    output.addLine("Y-Drehung: "+y);
    output.addLine("Z-Drehung: "+z);
  });
});